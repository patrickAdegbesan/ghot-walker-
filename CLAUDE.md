# Project instructions

## Git identity — IMPORTANT

All commits and pushes in this repository MUST be authored as the repository
owner, never as the AI assistant:

```
git config user.name "Patrick Adegbesan"
git config user.email "adegbesanpatrick14@gmail.com"
```

Set this config before the first commit of every session.

- Do NOT add `Co-Authored-By` trailers or session links to commit messages.
- Do NOT mention any AI assistant in commit messages, code comments,
  documentation, or any other file pushed to this repository.

## About this project

Final-year academic project: CardExpress, a web-based ATM card request and
delivery system (Flask + SQLite). See README.md for how to run it and
`tests/` for the pytest suite (`python -m pytest tests/ -v`).
